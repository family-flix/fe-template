import { ScrollViewCoreV2 } from "./index";

export function connect(store: ScrollViewCoreV2, $container: HTMLDivElement) {
//   store.scrollTo = (position: Partial<{ left: number; top: number }>) => {
//     $container.scrollTo(position);
//   };
}
